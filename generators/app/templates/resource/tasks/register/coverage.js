/**
 * CMD: coveralls
 *
 * ---------------------------------------------------------------
 *
 * Push coveralls to coveralls.io
 *
 */
module.exports = function(grunt) {
  grunt.registerTask('coverage', [
    'coveralls'
  ]);
};
